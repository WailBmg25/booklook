{
  "name": "Validate Python on Save",
  "description": "Automatically checks Python files for syntax errors and runs diagnostics when saved",
  "version": "1.0.0",
  "triggers": [
    {
      "type": "onFileSave",
      "enabled": true,
      "filePattern": "**/*.py"
    }
  ],
  "actions": [
    {
      "name": "Check Python Syntax and Diagnostics",
      "trigger": "onFileSave",
      "steps": [
        {
          "type": "agent",
          "prompt": "Check the saved file {{file.path}} for any syntax errors, type issues, or linting problems using getDiagnostics. If there are issues, provide a brief summary. If no issues found, just confirm the file is valid."
        }
      ]
    }
  ]
}
